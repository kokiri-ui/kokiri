import { NumberOrString as TabNavFlag, PlacementType as TabBarPosition } from '@petals/basic';

type TabNavType = {
  label?: string;
  disabled?: boolean;
  icon?: boolean;
};

export { TabNavFlag, TabNavType, TabBarPosition };
