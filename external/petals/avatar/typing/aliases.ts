import { SizeType } from '@petals/basic';
import { GraphicFitType as AvatarFit } from '@petals/graphic';

type AvatarShape = 'circle' | 'square';

type AvatarSize = SizeType | number;

export { AvatarShape, AvatarSize, AvatarFit };
